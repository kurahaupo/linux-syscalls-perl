Tools to generate Perl pack/unpack strings for a range of syscall structs.

In particular, stat, lstat, stat64, lstat64, and fstatat, which use structs
which depend strongly on the libc version and the target architecture.
